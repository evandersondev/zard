name: zard
description: Zard is a schema validation and transformation library for Dart, inspired by the popular Zod library for JavaScript.
version: 1.1.2
homepage: https://github.com/evandersondev/zard
repository: https://github.com/evandersondev/zard

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  string_normalizer: ^0.4.0

dev_dependencies:
  test: ^1.24.0
  benchmark_harness: ^2.2.0
